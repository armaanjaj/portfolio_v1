import React from "react";

export default function Idea({className = null}) {
    return (
        <div className={`font-extrabold text-transparent text-3xl bg-clip-text bg-gradient-to-r from-purple-400 to-pink-600 ${className && className}`}>
            !dea
        </div>
    );
}
